# Rapport — TP2

## Points clés

### 1. Sélections

const ... = document.querySelector ('...') --> place dans une variable les éléments sélectionnés dans le html

### 2. Intéractions au clic

la commande ... .addEventListener() prend en compte une action, exemple un clic : bouton.addEventListener('click', () => { --> lors du clic sur le bouton, le code qui suit est éxécuté. 

### 3. Gérer des classes

on peut ajouter/retirer des classes selon les évênements, 
... .classList.toggle() --> ajoute si la classe n'est pas présente, retire si elle y est déjà
... .classList.remove()--> retire la classe
... .classList.add() --> ajoute la classe
... .classList.contains() --> la variable contient la classe
 
## Utilisation de l'IA

Aucune IA utilisée lors de cette séance.
